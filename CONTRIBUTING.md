# JetBrains Rider Settings

The following Rider code formatting options are not overridable in .editorconfig and must be set manually.

* Settings -> Editor -> Code Style -> C# -> XML Documentation -> Indent inside element that contains text -> “No indent from parent element”