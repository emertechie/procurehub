
- If referencing a Key Vault secret, ensure to use a versioned URI to ensure it always uses the latest version if a new secret is created. See usage of `postgres_admin_password_secret_uri_with_version` for an example.
- 